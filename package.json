{
  "name": "des-moines-activities",
  "version": "1.0.0",
  "description": "Weekly newsletter for Des Moines activities",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "nodemon server/server.js",
    "update-activities": "node server/jobs/weeklyUpdate.js",
    "heroku-postbuild": "mkdir -p client && cp -r index.html styles.css script.js images client/"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.18.2",
    "mongoose": "^7.8.7"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
